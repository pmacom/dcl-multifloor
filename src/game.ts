import { Base } from "./base";
import { FloorButtons } from "./floorbuttons";
import { floorController } from "./floorcontroller";

const base = new Base()

const floorButtons1 = new FloorButtons(new Vector3(13, 1, 8))
const floorButtons2 = new FloorButtons(new Vector3(13, 5.5, 8))
const floorButtons3 = new FloorButtons(new Vector3(13, 9, 8))
